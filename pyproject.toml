[project]
name = "expense-tracker"
version = "0.1.0"
description = "A web-based expense tracker built with Python and Flask"
authors = [{name="Waithira-Felix-coder"}]
dependencies = [
    "matplotlib",
    "flask"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
